@*CSS*@
@*<link rel="stylesheet" href="~/Content/AdminCss/cssPsnManage.css" />*@
<link rel="stylesheet" href="~/Content/AdminCss/cssAdvManage.css" />



@model IEnumerable<CardAuction.Models.tAdminAd>

@{
    ViewBag.Title = "AdvManage";
}

<div class="slideshow-container">

    <div class="mySlides">
        <div class="numbertext">1 / 3</div>
        <img src="~/images/User/Index/SlideShowAD/1.jpg" style="width:100%">
        <div class="text">Caption Text</div>
    </div>

    <div class="mySlides">
        <div class="numbertext">2 / 3</div>
        <img src="~/images/User/Index/SlideShowAD/2.jpg" style="width:100%">
        <div class="text">Caption Two</div>
    </div>

    <div class="mySlides">
        <div class="numbertext">3 / 3</div>
        <img src="~/images/User/Index/SlideShowAD/3.jpg" style="width:100%">
        <div class="text">Caption Three</div>
    </div>

    <a class="prev" onclick="plusSlides(-1)">&#10094;</a>
    <a class="next" onclick="plusSlides(1)">&#10095;</a>
</div>

<br>

<div style="text-align:center">
    <span class="dot" onclick="currentSlide(1)"></span>
    <span class="dot" onclick="currentSlide(2)"></span>
    <span class="dot" onclick="currentSlide(3)"></span>
</div>



@*@using (Html.BeginForm())
{
    <p>
        Adver Name
        <input type="text" name="txtKeyword" />
        <input type="submit" value="查詢" />
    </p>
}*@

<p>
    @Html.ActionLink("新增廣告", "AdvCreate")
</p>
<table class="table">
    <tr>
        <th>
            No.
        </th>
        <th>
            Image
        </th>
        <th>
            Advert
        </th>
        <th>
            Note
        </th>
        <th>
            File Name
        </th>
        <th>
            Manage
        </th>
    </tr>

    @{
        int count = 0;
        foreach (var item in Model)
        {
            count++;
            <tr>
                <td>
                    @count
                </td>
                <td>
                    <img src="~/Images/User/Index/SlideShowAD/@item.fAdFileName" width="500" height="100" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.fAdName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.fAdNote)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.fAdFileName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "AdvEdit", new { id = item.fAdId }) |
                    @Html.ActionLink("Delete", "AdvDelete", "Admin", new { id = item.fAdId }, new { onclick = "return confirm('確定要刪除嗎?')" })
                </td>
            </tr>
        }
    }

</table>


<!-- 首頁幻燈片廣告圖片功能 JS -->
<script>
    var slideIndex = 1;
    showSlides(slideIndex);

    function plusSlides(n) {
        showSlides(slideIndex += n);
    }
    function currentSlide(n) {
        showSlides(slideIndex = n);
    }
    function showSlides(n) {
        var i;
        var slides = document.getElementsByClassName("mySlides");
        var dots = document.getElementsByClassName("dot");
        if (n > slides.length) { slideIndex = 1 }
        if (n < 1) { slideIndex = slides.length }
        for (i = 0; i < slides.length; i++) {
            slides[i].style.display = "none";
        }
        for (i = 0; i < dots.length; i++) {
            dots[i].className = dots[i].className.replace(" active", "");
        }
        slides[slideIndex - 1].style.display = "block";
        dots[slideIndex - 1].className += " active";
    }
</script>